<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme (also used as the context theme when inflating
         the WindowManager overlay so Material widgets resolve correctly). -->
    <style name="Theme.AutoSilentTimer" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/brand_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/brand_on_primary_container</item>
        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- Translucent variant for the overlay window: the WindowManager scrim
         (FLAG_DIM_BEHIND) dims the app behind, so this view itself is clear. -->
    <style name="Theme.AutoSilentTimer.Overlay" parent="Theme.AutoSilentTimer">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
